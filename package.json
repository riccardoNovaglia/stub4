{
  "name": "@stub4/stub4",
  "version": "1.0.0",
  "description": "We'll see",
  "repository": "https://github.com/riccardoNovaglia/stub4",
  "main": "./server/src/index.js",
  "scripts": {
    "test": "cd server && npm test",
    "start-client": "cd client && npm run start",
    "sc": "npm run start-client",
    "start-server": "cd server && npm run start",
    "ss": "npm run start-server",
    "prebuild": "npm run cleanup-build",
    "cleanup-build": "rm -rf ./lib && mkdir lib && rm -rf ./dist && mkdir dist",
    "build": "npm run build:server && npm run build:client",
    "build:client": "cd client  && npm run build",
    "build:server": "cd server  && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pact-foundation/pact": "^9.1.1",
    "axios": "^0.19.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "husky": "^3.0.3",
    "nodemon": "^1.19.3",
    "prettier": "^1.18.2",
    "pretty-quick": "^1.11.1",
    "supertest": "^3.4.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "bin": {
    "stub4": "./lib/bin.js"
  }
}
